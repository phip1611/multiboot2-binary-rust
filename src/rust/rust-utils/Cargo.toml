[package]
name = "utils"
description = "A few common utils. I made them separate to test them easilier."
version = "0.1.0"
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
